// Set this to the Cloudflare Worker URL during deployment. It is intentionally
// public: authentication happens through Cloudflare Access, not this URL.
window.PAPER_COLLECTOR_FEEDBACK_ENDPOINT = null;
